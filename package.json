{
  "name": "prototype",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.1.5",
    "bootstrap": "^4.3.1",
    "meteor-node-stubs": "^0.4.1",
    "simpl-schema": "^1.5.5"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
